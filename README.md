# Musical-C 🎵

> **Your terminal, now with rhythm.**

**Musical-cpp** is a lightweight, no-nonsense audio player built for the command line. Powered by the robust `miniaudio` library, it strips away the bloated GUIs to deliver pure sound directly from your source code to your speakers. 

Whether you're coding late into the night or just prefer the CLI lifestyle, this is your soundtrack engine.

## ⚡ Features

* **Minimalist Core**: Built on C++ and the single-header `miniaudio` library for efficiency.
* **Zero Bloat**: No electron apps, no heavy frameworks—just compiled binaries.
* **MP3 Support**: Native playback for your local library.
* **Cross-Platform**: Compiles cleanly on Linux, macOS, and Windows.

## 🛠️ Build & Install

Get the code, compile it, and play.

### Prerequisites
* A C++ compiler (`g++` or `clang++`)
* `make`

### Installation

1.  **Clone the repo**
    ```bash
    git clone [https://github.com/stremixx/Musical-cpp.git](https://github.com/stremixx/Musical-cpp.git)
    cd Musical-cpp
    ```

2.  **Compile**
    ```bash
    make
    ```

## 🚀 Usage

Run the player directly from your terminal. 

```bash
